node_modules/
build/
.env
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Auto-generated by scripts/generate-build-meta.js
src/buildMeta.js

# Diagnostic / ad-hoc export artifacts (the test suite never writes these
# to disk — it only exercises XLSX.write() in memory — but local
# repro scripts can occasionally leave one behind at the repo root)
/*.xlsx
/*.csv